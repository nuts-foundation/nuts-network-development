apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: demo-ehr-ingress
spec:
  rules:
  - http:
      paths:
      - path: /*
        backend:
          serviceName: demo-ehr-service
          servicePort: 80
